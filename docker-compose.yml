services:
  bot:
    build: .
    container_name: remnabuy_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - remnawave-network

networks:
  remnawave-network:
    external: true

